{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting migrations...' && alembic upgrade head && echo 'Migrations complete, starting server on port' $PORT && python -c 'from src.bot_ebooks.main import app; print(\"App imported successfully\")' && uvicorn src.bot_ebooks.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
